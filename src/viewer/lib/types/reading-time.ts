// Reading-time estimate for the breadcrumb: a word count and its ≈220 wpm minute figure.
// Produced by readingTimeFromWords (content/toc.ts), read by DocRenderer.show (content/doc-renderer.ts) for the breadcrumb.
interface ReadingTime {
  words: number;
  minutes: number;
}
