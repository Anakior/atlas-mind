{
  "name": "atlas-jsfmt",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only formatter for viewer/lib/*.js + sw.js. Pipeline: prettier (base layout) then eslint --fix (padding-line-between-statements = breathing blank lines after declarations, before return, around blocks, between functions). NOT shipped — the Atlas runtime is Python stdlib only. Install once (npm install), then `npm run format` after editing the viewer JS.",
  "scripts": {
    "format": "prettier --config .prettierrc.json --write ../lib/*.js ../sw.js && eslint --fix ../lib/*.js ../sw.js"
  },
  "devDependencies": {
    "prettier": "3.4.2",
    "eslint": "9.17.0"
  }
}
