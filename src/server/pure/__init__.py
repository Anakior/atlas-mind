"""Pure, stateless logic (dependencies passed explicitly): document path/traversal
(docs.py), token/CSRF/share crypto (auth.py), todo + note markdown (todos_md.py,
notes_md.py), federation helpers (hive.py), MCP tool dispatch (mcp_call.py).
"""
